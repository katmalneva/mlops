repos:
  - repo: local
    hooks:
      - id: unit-tests
        name: Run unit tests
        entry: bash -c 'PYTHONPATH=src python -m unittest discover -s tests'
        language: system
        pass_filenames: false